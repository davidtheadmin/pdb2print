# Extra dependencies for the FastAPI front end (server.py).
# These are on top of the existing pdb2print requirements.txt (geometry core).
# python-multipart is required by FastAPI to parse the multipart upload form.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
